---

- name: Register Monit's Executable Location
  command: command -v monit
  register: _monit_executable

- include_tasks: config.yml
  tags: monit_config
  become: true

- include_tasks: monitors.yml
  tags: monit_monitors
  become: true
