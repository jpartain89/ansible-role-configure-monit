---

monit_dir: /usr/local/etc/monit
monitrc_conf: "{{ monit_dir }}rc"
monit_includes: "{{ monit_dir }}/conf.d"
monit_log_destination: /usr/local/var/log/monit.log

monit_owner: root
monit_group: wheel
