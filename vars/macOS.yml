---

monit_dir: /usr/local/etc/monit
monitrc_conf: "{{ monit_dir }}rc"
monit_includes: "{{ monit_dir }}/conf.d"
monit_log_dir: /usr/local/var/log/monit
monit_log_destination: "{{ monit_log_dir }}/monit.log"

monit_lib_folder: /usr/local/var/lib/monit

monit_owner: root
monit_group: wheel
