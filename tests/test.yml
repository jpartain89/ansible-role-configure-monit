---
- hosts: localhost
  become: true

  vars:
    monit_webinterface_acl_rules:
      - localhost
    monit_services:
      - name: systemload
        type: system
        target: $HOST

  roles:
    - ansible-role-configure-monit
